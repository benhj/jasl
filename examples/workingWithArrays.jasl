// How to use arrays. Note incomplete. 
// Only the ability to make them currently.

start {
    
    prn "Creating some arrays";
    array:int(5) -> a;
    array:real(10) -> d;
    prn "Arrays created";

}