;;; Playing around with some different syntax ideas
;;; This one borrows a bit from C, but kind of back-to-front
;;; in a way.

start {
    
    int 1 -> i;
    int 1 -> j;
    ? (i = j) {
        prn "yes";
    }

    ? (i /= j) {
        prn "A";
    } : {
        prn "B";
    }
    
}
