returnable int factorial(n) -> f {
    
    ? (n > 1) {
        call factorial((n - 1)) -> result;
        int (n * result) -> f;
    } : {
        int 1 -> f;
    }
    
    return f;

}

start {

    args 0 -> arg;
    int arg -> N;

    call factorial(N) -> result;
    echo_nl result;

}
